# Core Python dependencies (pip install -r requirements.txt)
pandas>=1.3
numpy>=1.21
scipy>=1.7

# MOFA+ factor training (choose one):
#   Python : pip install mofapy2
#   R/Bioconductor: BiocManager::install("MOFA2")
# (Trained MOFA outputs are already provided in processed/, so training is optional.)

# External command-line tools (install separately, NOT pip):
#   salmon      : https://combine-lab.github.io/salmon/
#   sratoolkit  : https://github.com/ncbi/sra-tools (provides fasterq-dump)
